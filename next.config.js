// নতুন এবং সংশোধিত next.config.js ফাইল

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // আপনার যদি অন্য কোনো কনফিগারেশন থাকে, সেগুলো এখানে থাকবে
};

module.exports = nextConfig;